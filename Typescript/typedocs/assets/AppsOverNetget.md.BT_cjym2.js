import{_ as o,o as a,c as t,ae as s}from"./chunks/framework.BThPlfjz.js";const m=JSON.parse('{"title":"Apps Over Netget","description":"","frontmatter":{},"headers":[],"relativePath":"AppsOverNetget.md","filePath":"AppsOverNetget.md"}'),n={name:"AppsOverNetget.md"};function i(d,e,r,c,l,p){return a(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="apps-over-netget" tabindex="-1">Apps Over Netget <a class="header-anchor" href="#apps-over-netget" aria-label="Permalink to &quot;Apps Over Netget&quot;">​</a></h1><p>Proof-of-concept doc. Written the day the circuit first worked end-to-end (2026-08-18/19), so it doesn&#39;t decay into oral session knowledge.</p><p><strong>See also</strong> — this spans three packages; each has its own piece of the contract, cross-linked from here rather than duplicated:</p><ul><li><a href="https://neurons-me.github.io/monad/Typescript/typedocs/NRP-v0.3.0.html#9-gateway-binding-notes" target="_blank" rel="noreferrer">NRP v0.3.0 §9</a> — where <code>/apps/:name</code>/<code>/monads/:name</code> sit in the gateway binding.</li><li><a href="https://neurons-me.github.io/monad/Typescript/typedocs/NRP-v0.3.0.html#11-websocket-binding-nrp" target="_blank" rel="noreferrer">NRP v0.3.0 §11</a> — the full <code>/nrp</code> WebSocket message contract (<code>nrp.open</code>, <code>read</code>, <code>subscribe</code>, <code>data</code>, <code>stream</code>, …) that the &quot;live-semantic loop&quot; section below only summarizes.</li><li><a href="https://neurons-me.github.io/monad/Typescript/typedocs/Mesh/status.html#current-websocket-surface" target="_blank" rel="noreferrer">Mesh/status.md</a> — current implementation status and known gaps for the monad side.</li><li><a href="https://github.com/neurons-me/GUI/blob/main/Typescript/Runtime-Contract.md#me-runtime-live-over-a-network-monad" target="_blank" rel="noreferrer">this.gui Runtime-Contract §1</a> — the client-side <code>createWsMeRuntime()</code> contract (not part of a built docs site — GUI&#39;s typedocs doesn&#39;t include this file yet).</li></ul><hr><h2 id="what-this-is-a-proof-of-concept-of" tabindex="-1">What this is a proof of concept of <a class="header-anchor" href="#what-this-is-a-proof-of-concept-of" aria-label="Permalink to &quot;What this is a proof of concept of&quot;">​</a></h2><p>Not &quot;an app can reach a URL through netget&quot; — that part already worked. What&#39;s new: a normal <code>this.gui</code> React app can have <strong>live, cross-client <code>.me</code> state</strong> — reads and writes over HTTP, invalidation over WebSocket — addressed through netget&#39;s existing mesh by <strong>name</strong>, not by giving the app its own subdomain, using infrastructure that already existed but had never been exercised end-to-end (the WS-upgrade proxy headers, the <code>/monads/:name</code> path proxy, the <code>/nrp</code> WebSocket handler&#39;s resolution handshake). This closes the gap between &quot;the pieces exist&quot; and &quot;the pieces actually work together,&quot; verified with three independent clients (a <code>curl</code> write from outside the browser, and two open browser tabs) landing on the same live state with zero manual refresh:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>curl (external write)</span></span>
<span class="line"><span>  → netget: local.netget/apps/fulltrailer</span></span>
<span class="line"><span>  → monad: POST / (rootCommandHandler)</span></span>
<span class="line"><span>  → .me kernel write + pathNotify.notify()</span></span>
<span class="line"><span>  → /nrp WebSocket: &#39;stream&#39; push</span></span>
<span class="line"><span>  → this.gui: createWsMeRuntime()&#39;s subscribe callback</span></span>
<span class="line"><span>  → useSyncExternalStore</span></span>
<span class="line"><span>  → two independent React tabs re-render, in sync, unprompted</span></span></code></pre></div><p><strong>This is a first working circuit, not a finished platform.</strong> See &quot;Known gaps&quot; below before building a second app on this pattern.</p><hr><h2 id="why-apps-name-not-a-subdomain" tabindex="-1">Why <code>/apps/:name</code>, not a subdomain <a class="header-anchor" href="#why-apps-name-not-a-subdomain" aria-label="Permalink to &quot;Why \`/apps/:name\`, not a subdomain&quot;">​</a></h2><p>The first instinct was to give each app its own DNS-shaped identity — <code>fulltrailer.&lt;machine-hostname&gt;</code> — matching how personal namespaces already work (<code>jabellae.&lt;machine-hostname&gt;</code>). Two problems, found by actually building it:</p><ol><li><p>It needs an explicit <code>/etc/hosts</code> entry per app (netget already does this per-user, via <code>addLocalHostEntry</code>) — a real, <code>sudo</code>-gated, per-machine setup step for every new app, every new dev machine.</p></li><li><p>It reads like it should be backed by NRP&#39;s <code>[selector]</code> bracket syntax — it isn&#39;t. Read the spec closely (<code>modules/monad/Typescript/typedocs/NRP-v0.3.0.md</code>): <code>selector</code> in <code>me://namespace[selector]/path</code> picks a <strong>monad/device instance for an already-existing namespace</strong> —</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[monadlisa]/profile</span></span>
<span class="line"><span>me://jabellae.cleaker.me[monadluis]/profile</span></span></code></pre></div><p><code>monadlisa</code>/<code>monadluis</code> are two of jabellae&#39;s <em>devices</em>, both answering for the <em>same</em> namespace <code>jabellae.cleaker.me</code>. There is no NRP construct for &quot;address a different app without giving it its own namespace&quot; — the selector doesn&#39;t substitute for the subdomain, it operates one level inside it.</p></li></ol><p>What actually solves it: netget already has a <strong>path-based</strong> proxy, <code>/monads/:name</code> (<code>lua/handlers/monad_proxy.lua</code>), that looks up a live <code>app.id</code>/<code>monadName</code> in the local mesh registry (<code>apps.json</code>) and reverse proxies to its <code>host:port</code> — <strong>stripping the <code>/monads/:name</code> prefix before forwarding</strong> (<code>ngx.req.set_uri(tail, false)</code>), so the target process sees clean root-relative paths (<code>/</code>, <code>/me/*</code>, <code>/nrp</code>) exactly as if addressed directly. No subdomain, no <code>/etc/hosts</code> entry, no DNS at all — just a name that has to already be registered (heartbeating) in the mesh.</p><p><code>/monads/:name</code> describes the <em>mechanism</em> (which monad answers). Added a sibling route, <strong><code>/apps/:name</code></strong> (same handler, same registry lookup — see <code>setNginxConfigRoutes.ts</code>), because from a user/admin perspective FullTrailer isn&#39;t &quot;a monad,&quot; it&#39;s an app that happens to be <em>implemented</em> as one today. <strong><code>/monads/:name</code> stays as the internal/infra/debug route. GUI code, templates, and docs should only ever reference <code>/apps/:name</code>.</strong> This also leaves room for a future <code>/apps/:name</code> target that <em>isn&#39;t</em> a monad directly (a static surface, netget&#39;s own built-in admin UI) without renaming anything public-facing.</p><h3 id="the-namespace-resolution-gotcha-this-creates" tabindex="-1">The namespace-resolution gotcha this creates <a class="header-anchor" href="#the-namespace-resolution-gotcha-this-creates" aria-label="Permalink to &quot;The namespace-resolution gotcha this creates&quot;">​</a></h3><p>A monad resolves which <code>.me</code> namespace a request is for from the <strong>Host header</strong> (<code>http/namespace.ts</code>&#39;s <code>resolveNamespace</code>). Proxying through <code>local.netget/apps/fulltrailer/...</code> means the monad sees <code>Host: local.netget</code> — which does not literally equal its own <code>ME_NAMESPACE</code> (<code>fulltrailer.&lt;machine-hostname&gt;</code>), so naively it resolves to the <em>wrong</em> namespace (literally <code>&quot;local.netget&quot;</code>).</p><p>The fix already existed, unused: <code>readLocalIdentityNamespace()</code> aliases <em>any</em> hostname listed in <code>MONAD_SELF_HOSTNAME</code> / <code>MONAD_SELF_ENDPOINT</code> / <code>MONAD_SELF_TAGS</code> (comma-separated env var) to the monad&#39;s own <code>ME_NAMESPACE</code>. Every app&#39;s monad needs <code>MONAD_SELF_TAGS</code> to include every mesh host it might be reached through (<code>local.netget</code>, the machine&#39;s bare hostname, <code>127.0.0.1</code>, …) — see <code>apps/FullTrailer/monad/.env</code>. This is manual, per-app, per-machine configuration today — see &quot;Known gaps.&quot;</p><hr><h2 id="the-three-layers" tabindex="-1">The three layers <a class="header-anchor" href="#the-three-layers" aria-label="Permalink to &quot;The three layers&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>this.gui (browser)  ──HTTP+WS──▶  netget (switchboard)  ──HTTP+WS──▶  monad (daemon)</span></span>
<span class="line"><span>createWsMeRuntime()                /apps/:name proxy,        rootCommandHandler,</span></span>
<span class="line"><span>                                    strips prefix,             pathResolver.ts,</span></span>
<span class="line"><span>                                    CORS headers,               /nrp WebSocket</span></span>
<span class="line"><span>                                    no app logic</span></span></code></pre></div><ul><li><strong>netget</strong> does pure name resolution and reverse proxying. It has no opinion about what&#39;s behind <code>/apps/:name</code> — today it&#39;s always a monad, but nothing in the routing layer assumes that.</li><li><strong>monad</strong> (<code>modules/monad/Typescript</code>) is a generic, unmodified daemon — every app gets its <strong>own instance</strong> of the <em>same</em> codebase, configured via env vars and a <code>self.json</code>, not a fork. FullTrailer&#39;s app-specific business logic (a Samsara fuel-telematics proxy, photo uploads) stays in its <strong>own</strong>, separate Express process — the monad&#39;s <code>app.ts</code> is a fixed router chain with no extension point for custom routes, and that&#39;s deliberate: folding app-specific logic into the shared daemon would make every future app carry every other app&#39;s routes.</li><li><strong>this.gui</strong> (<code>createWsMeRuntime</code>) is the client adapter — the only piece that&#39;s genuinely new code, not existing infrastructure finally wired up.</li></ul><hr><h2 id="the-live-semantic-loop-in-detail" tabindex="-1">The live-semantic loop, in detail <a class="header-anchor" href="#the-live-semantic-loop-in-detail" aria-label="Permalink to &quot;The live-semantic loop, in detail&quot;">​</a></h2><h3 id="server-side-generic-—-lives-in-modules-monad-typescript-benefits-every-app" tabindex="-1">Server side (generic — lives in <code>modules/monad/Typescript</code>, benefits every app) <a class="header-anchor" href="#server-side-generic-—-lives-in-modules-monad-typescript-benefits-every-app" aria-label="Permalink to &quot;Server side (generic — lives in \`modules/monad/Typescript\`, benefits every app)&quot;">​</a></h3><ul><li><code>kernel/pathNotify.ts</code> — new, small, in-process pub/sub: <code>subscribe(namespace, path, cb)</code> / <code>notify(namespace, path)</code>. Matches a write to a subscriber if paths are equal or one is a dotted-prefix of the other. Single-process only — see &quot;Known gaps.&quot;</li><li>Hooked into <code>handlers/commandHandler.ts</code>&#39;s <code>rootCommandHandler</code> (the real <code>POST /</code> write path — <strong>not</strong> <code>meCommandHandler</code>, which only handles kernel <code>claim</code>/<code>open</code>, a naming trap worth remembering) right after the existing <code>saveSnapshot()</code> call.</li><li><code>http/nrpHandler.ts</code>&#39;s <code>/nrp</code> WebSocket handler gained <code>read</code> / <code>subscribe</code> / <code>unsubscribe</code> client message types (alongside the pre-existing <code>nrp.open</code> → <code>resolved</code> resolution handshake, previously the <em>only</em> thing this socket did — used by <code>this.gui</code>&#39;s <code>Beatle</code> component, which had zero real callers anywhere before this). On <code>subscribe</code>, replies immediately with the current value (<code>data</code>), then pushes a <code>stream</code> message with the fresh value every time <code>pathNotify</code> fires for that path, until <code>unsubscribe</code> or the socket closes.</li></ul><h3 id="client-side-generic-—-lives-in-this-gui-published-as-2-3-0" tabindex="-1">Client side (generic — lives in <code>this.gui</code>, published as <code>2.3.0</code>) <a class="header-anchor" href="#client-side-generic-—-lives-in-this-gui-published-as-2-3-0" aria-label="Permalink to &quot;Client side (generic — lives in \`this.gui\`, published as \`2.3.0\`)&quot;">​</a></h3><ul><li><code>createWsMeRuntime(me, {semanticNamespace, transportOrigin})</code> — a <code>RuntimeAdapter</code>. Reads/writes go over HTTP (<code>this.gui</code>&#39;s own <code>monadClient.ts</code>); <code>subscribe</code>/<code>getSnapshot</code> ride one shared <code>/nrp</code> WebSocket connection, deduped per path across however many local <code>useMeValue</code>/<code>{read: ...}</code> callers are watching it. An incoming live value gets written into the <strong>local</strong> <code>me</code> object (<code>writeMeValue</code>) and a local store notified — <code>useMeValue</code>&#39;s <code>getSnapshot</code> always reads straight from <code>me</code>, not from the adapter, so this is what actually makes a live update visible to a component, not a side detail.</li><li>Must be passed <strong>explicitly</strong> — to <code>MeRuntimeProvider</code>&#39;s <code>runtime</code> prop, and separately to any standalone <code>SpecBoundary</code> (not context-aware on its own; pull it via <code>useOptionalMeRuntimeContext()?.runtime</code>). Omitting it silently falls back to a no-op identity adapter — same visible symptom (a <code>{read: ...}</code> token echoes its own path string) as forgetting the <code>me/</code> prefix on the path itself (the renderer&#39;s expression allowlist blocks anything else). Two different bugs, same symptom — check both.</li></ul><hr><h2 id="recipe-wiring-a-new-app-this-way" tabindex="-1">Recipe: wiring a new app this way <a class="header-anchor" href="#recipe-wiring-a-new-app-this-way" aria-label="Permalink to &quot;Recipe: wiring a new app this way&quot;">​</a></h2><p>FullTrailer did every step of this by hand; nothing below is automated yet (see &quot;Known gaps&quot; — this is exactly what templating should eventually generate).</p><ol><li>Pick a name (<code>myapp</code>). Generate a fresh <code>SEED</code> (<code>openssl rand -hex 32</code>) — don&#39;t reuse another app&#39;s or a personal identity&#39;s seed.</li><li><code>myapp/monad/self.json</code> — <code>identity: &quot;myapp.&lt;machine-hostname&gt;&quot;</code>, <code>monadName: &quot;myapp&quot;</code>, its own <code>endpoint</code>/port.</li><li><code>myapp/monad/.env</code> — <code>SEED</code>, <code>ME_NAMESPACE</code> (matches <code>self.json</code>&#39;s <code>identity</code>), <code>MONAD_NAME</code>, <code>PORT</code> (pick one not already in use), <code>ME_STATE_DIR</code> and <code>MONAD_SELF_CONFIG_PATH</code> as <strong>absolute</strong> paths (the monad process runs from its own checkout&#39;s directory, not the app&#39;s — relative paths resolve against the wrong cwd), <code>MONAD_SELF_TAGS</code> listing every mesh host this app should answer to (<code>local.netget</code>, the machine&#39;s bare hostname, <code>127.0.0.1</code>).</li><li>A small run script that sources <code>.env</code> and runs <code>modules/monad/Typescript</code>&#39;s <code>tsx watch server.ts</code> from <em>its own</em> directory — don&#39;t copy the monad codebase into the app&#39;s repo.</li><li>Frontend: <code>createWsMeRuntime(me, { semanticNamespace: &#39;myapp.&lt;machine-hostname&gt;&#39;, transportOrigin: &#39;http://local.netget/apps/myapp&#39; })</code>, passed to <code>MeRuntimeProvider</code> (and to any <code>SpecBoundary</code> used outside the package&#39;s own <code>mountApp()</code>).</li><li>Regenerate + reload netget&#39;s config so the new <code>/apps/myapp</code> traffic is nothing new — the route already exists generically, this step is only needed the first time <code>/apps/:name</code> itself is added to a given gateway install, not per-app.</li><li>Verify with the exact three-client test this doc opens with, before trusting it: an external <code>curl</code> write, two open tabs, no refresh.</li></ol><hr><h2 id="known-gaps-—-read-before-building-a-second-app-on-this" tabindex="-1">Known gaps — read before building a second app on this <a class="header-anchor" href="#known-gaps-—-read-before-building-a-second-app-on-this" aria-label="Permalink to &quot;Known gaps — read before building a second app on this&quot;">​</a></h2><ul><li><strong>No real auth beyond loopback trust.</strong> <code>/apps/:name</code> and <code>/monads/:name</code> rely on the same trust model every other loopback-only netget route uses today. Fine for local dev, not evaluated for anything beyond that.</li><li><strong><code>MONAD_SELF_TAGS</code> is manual, per-app, per-machine.</strong> Nothing generates or validates it; get it wrong and writes silently land in a namespace named after whatever Host header happened to reach the monad.</li><li><strong><code>pathNotify</code> is single-process, in-memory.</strong> No fan-out across multiple monad instances or processes. Fine for &quot;one app, one monad, one machine&quot;; not a distributed pub/sub.</li><li><strong>No WebSocket write path.</strong> Writes are HTTP-only, applied optimistically to the local <code>me</code> object before the network call resolves. A failed write is logged to the console, not surfaced to the UI — no retry, no rollback of the optimistic value.</li><li><strong>Not templated.</strong> <code>npx this.gui my-app</code> does not generate any of this yet — every piece in the recipe above was hand-wired for FullTrailer.</li><li><strong>This doc, not code, is the only place the <code>/monads/:name</code> vs. <code>/apps/:name</code> naming decision is recorded.</strong> If <code>/apps/:name</code> ever grows a backing implementation that isn&#39;t <code>monad_proxy.lua</code>, update this doc in the same change.</li></ul>`,35)])])}const u=o(n,[["render",i]]);export{m as __pageData,u as default};
